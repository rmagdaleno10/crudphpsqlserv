{% extends 'layout.twig' %}

{% block title %}Página de Inicio{% endblock %}

{% block content %}
    <h2>{{ pagina_titulo }}</h2>
    <p>{{ contenido }}</p>
    <pre>
        {{ tasks }}
    </pre>
{% endblock %}
